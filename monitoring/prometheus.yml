global:
  scrape_interval: 10s
  evaluation_interval: 10s

scrape_configs:
  - job_name: 'ml-platform-api'
    static_configs:
      # host.docker.internal reaches the host machine from inside Docker on macOS
      - targets: ['host.docker.internal:8000']
    metrics_path: '/metrics'
